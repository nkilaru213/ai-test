# Endpoint AI Demo

A small ChatGPT-style demo app for endpoint/PC support Q&A.

## Setup

```bash
git clone <your-repo> endpoint-ai-demo   # or copy folder
cd endpoint-ai-demo
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
